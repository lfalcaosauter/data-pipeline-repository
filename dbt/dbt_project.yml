name: 'banking_dbt'
version: '1.0.0'
config-version: 2

profile: 'banking_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  banking_dbt:
    staging:
      +materialized: view

    gold:
      +materialized: table
